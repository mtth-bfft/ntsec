#include <Windows.h>
#include <tchar.h>
#include <stdio.h>
#include "files.h"
#include "utils.h"

int enumerate_files_with(DWORD dwDesiredAccess)
{
   int res = 0;
   (void)(dwDesiredAccess);
   return res;
}